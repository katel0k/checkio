ARG PORT=6000
ARG NAME=checkers_db

FROM postgres

ENV POSTGRES_USERNAME=admin
ENV POSTGRES_PASSWORD=admin
ENV POSTGRES_DB=$NAME
ENV POSTGRES_PORT=$PORT
ENV POSTGRES_HOST_AUTH_METHOD=trust




EXPOSE $PORT